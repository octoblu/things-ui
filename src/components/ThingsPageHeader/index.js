import React, { PropTypes } from 'react'

import styles from './styles.css'

const propTypes = {}
const defaultProps = {}

const ThingsPageHeader = () => {
  return <div className={styles.root}>Things</div>
}

ThingsPageHeader.propTypes    = propTypes
ThingsPageHeader.defaultProps = defaultProps

export default ThingsPageHeader
